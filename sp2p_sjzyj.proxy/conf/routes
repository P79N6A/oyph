# This file defines all module routes (Higher priority routes first)
#
# import these routes in the main app as :
# *     /                       module:sp2p_sjzyj.proxy


#
# ~~~~
#
# 后台-登陆-页面 
GET  /proxy/?                                       	front.proxy.LoginCtrl.loginPre

#
# ~~~~
# 前台-代理商-登录控制器(controllers.front.proxy.SalesManCtrl)
GET  /front/proxy/login/{action}.html                      			front.proxy.LoginCtrl.{action}Pre
POST /front/proxy/login/{action}                           			front.proxy.LoginCtrl.{action}

#
# ~~~~
# 前台-代理商业务员-控制器(controllers.front.proxy.SalesManCtrl)
GET  /front/proxy/salesman/{action}.html                 front.proxy.SalesManCtrl.{action}Pre
POST /front/proxy/salesman/{action}                      front.proxy.SalesManCtrl.{action}

#
# ~~~~
# 前台-代理商-控制器(controllers.front.proxy.ProxyCtrl)
GET  /front/proxy/{action}.html                      			front.proxy.ProxyCtrl.{action}Pre
POST /front/proxy/{action}                           			front.proxy.ProxyCtrl.{action}

#
# ~~~~
# 前台-代理商业务员-控制器(controllers.front.proxy.ProxyCtrl)
GET  /supervisor/proxy/{action}.html                     back.proxy.BackProxyCtrl.{action}Pre
POST /supervisor/proxy/{action}                          back.proxy.BackProxyCtrl.{action}


# 财务项目理财提成接口
* /front/deduct/scanSales                  front.proxy.DeductInterface.scanSales
* /front/deduct/deductInfo                 front.proxy.DeductInterface.deductAllInfo
