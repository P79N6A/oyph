# This file defines all module routes (Higher priority routes first)
#
# import these routes in the main app as :
# *     /                       module:sp2p_sjzyj.extend.redpacket
#
# ~~~~

# 前台-账户中心-我的奖励-红包控制器(controllers.front.account.MyRedpacketCtrl)
GET  /account/reward/redpacket/{action}.html                   front.account.MyRedpacketCtrl.{action}Pre
POST /account/reward/redpacket/{action}                        front.account.MyRedpacketCtrl.{action}

# 前台-账户中心-我的奖励-加息券控制器(controllers.front.account.MyCouponCtrl)
GET  /account/reward/coupon/{action}.html                   	front.account.MyCouponCtrl.{action}Pre
POST /account/reward/coupon/{action}                        	front.account.MyCouponCtrl.{action}



# 后台-推广-红包控制器(controllers.back.spread.RedpacketCtrl)
GET  /supervisor/spread/redpacket/{action}.html                  back.spread.RedpacketCtrl.{action}Pre
POST /supervisor/spread/redpacket/{action}                       back.spread.RedpacketCtrl.{action}

# 后台-推广-签到控制器(controllers.back.spread.RedpacketCtrl)
GET  /supervisor/spread/sign/{action}.html                      back.spread.SignActCtrl.{action}Pre
POST /supervisor/spread/sign/{action}                           back.spread.SignActCtrl.{action}